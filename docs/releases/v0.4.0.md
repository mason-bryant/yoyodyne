# v0.4.0

Released 2026-08-30, covering v0.3.0 to v0.4.0.

## Key functionality

- **Rebuild multi-account pooling against the plugin contract** (`yoyodyne-ifd.191`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Make provider integration a plugin contract users can extend** (`yoyodyne-ifd.32`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **First-class external configuration: discovery order, user-level location, yoyo init --external** (`yoyodyne-ifd.77`)
- **Fork-based publishing: push run branches to a fork and open cross-repository pull requests** (`yoyodyne-ifd.79`)

### Critical fixes

- **A handback carries the preserved change, not a clean worktree** (`yoyodyne-ifd.178`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Worktree cleanup keeps the sandbox viable: prune stale registrations, bound the deny list** (`yoyodyne-ifd.151`)
  Serves: Isolate implementation tasks in harness-managed Git worktrees and integrate successful work automatically.

## Enhancements

- **Verify the promotion precondition the team-mode design leans on** (`yoyodyne-ifd.154`)
  Serves: Isolate implementation tasks in harness-managed Git worktrees and integrate successful work automatically.
- **Dependency links bind in-flight items: run-start and repair re-check what selection checked** (`yoyodyne-ifd.155`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Each cut writes release notes: key functionality, enhancements, fixes** (`yoyodyne-ifd.156`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Parked work is unschedulable: a status the scheduler respects, not a priority it drains to** (`yoyodyne-ifd.188`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The heartbeat names the resident's binary age, and staleness past a threshold reaches the DM tier** (`yoyodyne-ifd.189`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An operator-decided escalation can clear or raise an item's caps, recorded and attributed** (`yoyodyne-ifd.193`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A thread reply that becomes a directive is acknowledged in the thread** (`yoyodyne-ifd.68.19`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Every artifact home gets a README: purpose, owner, and the hand-edit policy** (`yoyodyne-ifd.125.5`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Decomposition from a failed run records the parent's landing as a dependency** (`yoyodyne-ifd.157`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **README.md is actually reduced against the extracted documents** (`yoyodyne-ifd.160`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **A duplicate provider terminal result relaunches like any transient death** (`yoyodyne-ifd.164`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The declared checks cover what the repository is made of, not only Go** (`yoyodyne-ifd.165`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Reconcile the docs-map disposition table before tranche 2 drops content** (`yoyodyne-ifd.166`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **An operational directive's outcome is recorded: admissions name the directive that prompted them** (`yoyodyne-ifd.168`)
  Serves: Make user directives durable, discoverable, and enforceable regardless of which agent received them.
- **Reconcile re-asks the forge about a failed run's publication** (`yoyodyne-ifd.170`)
  Serves: Publish that work as pull requests the harness opens, and has the forge merge, on the roles' behalf, for projects that enable it, without letting any agent push or merge.
- **Run listings say what happened and what remains, not one word for four outcomes** (`yoyodyne-ifd.173`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, and every distinction survives a terminal that cannot render emphasis.
- **The wedge's residue: recovery for pre-fix divergence, and the queued-merge early close** (`yoyodyne-ifd.181`)
  Serves: Isolate implementation tasks in harness-managed Git worktrees and integrate successful work automatically.
- **Every provider spend is one durable log line at the moment it is known** (`yoyodyne-ifd.182`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **A grant naming a provider-protected path is refused at admission, not discovered after the work** (`yoyodyne-ifd.185`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The artifact-home indexes, written with the granted pen: four READMEs and three corrected claims** (`yoyodyne-ifd.186`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Team mode: collaborators each run yoyo against one shared repository** (`yoyodyne-ifd.82`)
  Serves: A team can run Yoyodyne against one shared repository: collaborators each run their own harness without losing work, splitting the tracker, or weakening any safety invariant.
- **A conversation lease survives an interrupted close: Release tolerates EINTR** (`yoyodyne-ifd.161`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The composing region bounds itself to the window: erase cannot climb into the transcript** (`yoyodyne-ifd.169`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, and every distinction survives a terminal that cannot render emphasis.
- **yoyo cost reports the cache-read share, so the caching experiment can conclude** (`yoyodyne-ifd.171`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **Every priced terminal in a run's log attributes to its phase** (`yoyodyne-ifd.172`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **Aggregate cost reporting reads the exchange store** (`yoyodyne-ifd.175`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **Verify the default configuration excludes the invariants home from conversation writes** (`yoyodyne-ifd.176`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **The skill's behavioral claims are verified against the CLI they describe** (`yoyodyne-ifd.179`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Design a portable agent configuration system** (`yoyodyne-ifd.36`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Validate the built-in bundle's declared schema version** (`yoyodyne-ifd.4.3`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Record the output-emphasis discipline once, where every surface inherits it** (`yoyodyne-ifd.67`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, and every distinction survives a terminal that cannot render emphasis.
- **One voice for the product manager, and one table for what --message refuses** (`yoyodyne-ifd.81`)
- **Design distributed coordination for team mode** (`yoyodyne-ifd.82.2`)
  Serves: A team can run Yoyodyne against one shared repository: collaborators each run their own harness without losing work, splitting the tracker, or weakening any safety invariant.
- **Repository-coupled goal tests must not fail legitimate states** (`yoyodyne-ifd.89`)
- **Two minors from ifd.1.9's approving review: audit output symmetry and the exclusion rationale's home** (`yoyodyne-ifd.91`)
- **Assert the amendment payload survives the disk round-trip** (`yoyodyne-ifd.93`)

## Bug fixes

- **An empty-diff round is an environmental refusal: it spends no budget and counts toward no cap** (`yoyodyne-ifd.190`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **ifd.149 closed against work absent from the tree: re-verify, and land the guard for real** (`yoyodyne-ifd.180`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **A moved remote target cannot close an item as integrated into a state nothing resolves** (`yoyodyne-ifd.177`)
  Serves: Isolate implementation tasks in harness-managed Git worktrees and integrate successful work automatically.
- **The docket view still diverges from the triage ledger: third recurrence, after the fix** (`yoyodyne-ifd.163`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The attribution-destroying writer is guarded and the witness backfill runs** (`yoyodyne-ifd.149`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **The release gate commits the tracker's derived exports instead of refusing on them** (`yoyodyne-ifd.150`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
