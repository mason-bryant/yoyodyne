# v0.5.0

Covering v0.4.0 to v0.5.0: what landed on `main` either side of the 2026-09-08
to 2026-09-13 capacity stoppage, during which nothing could be cut. The release
date is the tag's.

## Key functionality

- **Model failover, configurable per agent: a role's turn is served by a permitted alternate** (`yoyodyne-ifd.331`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An optional pinned model version, falling back to the family's latest** (`yoyodyne-ifd.333`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Express Claude and Codex in the endpoint model** (`yoyodyne-ifd.340`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional, and support multiple providers behind one adapter contract.
- **One refusal-classification conformance suite every adapter passes** (`yoyodyne-ifd.342`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional, and support multiple providers behind one adapter contract.
- **Cross-provider failover, with the rebuild path exercised** (`yoyodyne-ifd.343`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional, and support multiple providers behind one adapter contract.
- **Land or redo the Codex adapter so the second endpoint can run** (`yoyodyne-ifd.347`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional, and support multiple providers behind one adapter contract.
- **An account names its provider, and a wrong-provider account is refused before a claim** (`yoyodyne-ifd.351`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional, and support multiple providers behind one adapter contract.
- **An approving review round does not spend the item's retry budgets** (`yoyodyne-ifd.279`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A refused rerun spends nothing: pre-flight precedes the budget increment** (`yoyodyne-ifd.299`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An item closes on the confirmed merge, and an outstanding publication is not pullable work** (`yoyodyne-ifd.301`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Unmeetable items do not dispatch: readiness checks prerequisites against the tree** (`yoyodyne-ifd.304`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A rerun carries the decision the development manager recorded, not words handed to a flag** (`yoyodyne-ifd.311`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **An escalate verdict: either role says 'this cannot work' and the DM decides** (`yoyodyne-ifd.303`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The shipped-documentation set is scoped to the repository it describes** (`yoyodyne-ifd.312`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Two claim-path bd behaviors are pinned against the real store** (`yoyodyne-ifd.285`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Declarative becomes the default; legacy retained with rollback documented** (`yoyodyne-ifd.209.7`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Project-owned workflow definitions: the graph as project-visible data** (`yoyodyne-ifd.209.8`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **A run lands evidence without closing its item: closure follows the discharging outcome only** (`yoyodyne-ifd.209.23`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.

### Critical fixes

- **An attempt that dies before its run record still records what happened** (`yoyodyne-ifd.356`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A run that dies before claiming is recorded, and readiness agrees with the claim gate** (`yoyodyne-ifd.338`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A run survives its own verbosity: output is truncated with a marker, not fatal** (`yoyodyne-ifd.258`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Recoverable failures retry with backoff at every publish and integration boundary** (`yoyodyne-ifd.264`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **State.Failure is bounded on the record itself** (`yoyodyne-ifd.317`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **ReviewSummary is bounded like Failure, and history stays loadable** (`yoyodyne-ifd.329`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A run's spawned processes die with the run, and background load wears a leash** (`yoyodyne-ifd.296`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.

## Enhancements

- **One durable interaction and state service: the shared read model and conversation seams** (`yoyodyne-ifd.139`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The advisory management-loop slice: restart, concurrency, and staleness proven** (`yoyodyne-ifd.142`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The four-line status: running, working, not-startable-with-reasons, needs-a-human** (`yoyodyne-ifd.194`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Workflow definition schema: versioned, digest-pinned, validated** (`yoyodyne-ifd.209.1`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Action and capability registries wrapping the existing pipeline steps** (`yoyodyne-ifd.209.2`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Commit and publish an approved artifact write** (`yoyodyne-ifd.100.1`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Land the conversation-hold seam: console and harness both reach the product manager** (`yoyodyne-ifd.130.2`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The architect words the invariant: a developer always verifies its own change before review** (`yoyodyne-ifd.183`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **A repair dispatch invokes the handback path, never a fresh run in its place** (`yoyodyne-ifd.195`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Harness-invoked sessions carry no plan-mode workflow: session mode is set per role** (`yoyodyne-ifd.199`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Land the 2-of-2 escalation branch with the budget constant reverted** (`yoyodyne-ifd.209.17`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A registered run.complete action: workflow definitions can express finishing the work** (`yoyodyne-ifd.209.18`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Strict definition loader and compiler: invalid definitions fail before claiming work** (`yoyodyne-ifd.209.3`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Checkpointed workflow execution: an instance runs a compiled graph durably** (`yoyodyne-ifd.209.4`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Authority inventory: every role-authority check in the harness, enumerated and pinned** (`yoyodyne-ifd.209.9`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Freeze the behavioral baseline: golden tests for what the delivery pipeline actually guarantees** (`yoyodyne-ifd.211`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **Every run and resident pins the harness build it ran, and the records prove deployment** (`yoyodyne-ifd.213`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **Land the watch self-redeploy: the reviewed change replayed onto current main** (`yoyodyne-ifd.230`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The idle line says what was passed over and names the conversation that moves next** (`yoyodyne-ifd.237`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A stall watchdog notices that nothing is happening and says so unprompted** (`yoyodyne-ifd.242`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A refused tracker block is recorded, carried, and shown back to the role that sent it** (`yoyodyne-ifd.246`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Every run death that preserves work leaves an executable docket entry** (`yoyodyne-ifd.268`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The watchdog and the status head read the usage-pause state** (`yoyodyne-ifd.290`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A refused tracker block wakes the role that sent it** (`yoyodyne-ifd.319`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The stall alarm reads the shared cause analysis instead of deriving its own** (`yoyodyne-ifd.324`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **The side-conversation stream: its own durable record and lease** (`yoyodyne-ifd.330.1`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The report pile is worked on a schedule, with a bound that converges** (`yoyodyne-ifd.350`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The app responds when @-mentioned: status questions get the status block, everything else gets one clear sentence** (`yoyodyne-ifd.68.24`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **README split tranche 3: reduce the README against the extracted documents** (`yoyodyne-ifd.121.5`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Reviews read the cached prefix instead of rewriting it every time** (`yoyodyne-ifd.205`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **Sweep coined terms out of tracker items and governed documents** (`yoyodyne-ifd.206`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **Role-capability registry: the five roles' capabilities as trusted data in code** (`yoyodyne-ifd.209.10`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Default bundles express the five roles through the capability vocabulary** (`yoyodyne-ifd.209.11`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Release readiness: a configured workflow checks the system against its goals before each tag** (`yoyodyne-ifd.209.15`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **A non-discharging landing re-parks its item; leave-open requires an explicit marker** (`yoyodyne-ifd.209.24`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **No closure route bypasses the landing's outcome kind** (`yoyodyne-ifd.209.26`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **A reviewer's approval states what it approves: implementation or evidence** (`yoyodyne-ifd.209.27`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **The delivery loop encoded as the built-in workflow definition** (`yoyodyne-ifd.209.5`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Opt-in parity soak: the declarative executor runs real work beside the legacy path** (`yoyodyne-ifd.209.6`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **The agent instructions match the sandbox: bd is not a developer-run tool** (`yoyodyne-ifd.214`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An operational directive can be withdrawn** (`yoyodyne-ifd.215`)
  Serves: Make user directives durable, discoverable, and enforceable regardless of which agent received them.
- **docs/terms.md: every coined term registered with a plain-word definition** (`yoyodyne-ifd.216`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **The installed maintenance job supervises Slack services, one per product, tokens from the keychain** (`yoyodyne-ifd.221`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A pull request says what it contains: governed item text and computed facts, not boilerplate** (`yoyodyne-ifd.222`)
  Serves: Publish that work as pull requests the harness opens, and has the forge merge, on the roles' behalf, for projects that enable it, without letting any agent push or merge.
- **A worktree gets the current tracker export at creation** (`yoyodyne-ifd.223`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **Each newly-available improvement DMs the operator once, deduplicated, per the ruling** (`yoyodyne-ifd.241.4`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Repair-request channel messages carry a one-line summary of each finding** (`yoyodyne-ifd.252`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A live smoke check pins every bd behavior the harness assumes** (`yoyodyne-ifd.267`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The product manager's coherence scan: twice daily, contradictions surfaced, questions on top** (`yoyodyne-ifd.284`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Reports render tight and honest: no warning without an ask, no post without a point** (`yoyodyne-ifd.314`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **Review evidence for a continued run spans the branch, not the worktree tail** (`yoyodyne-ifd.321`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Awaiting a decision and awaiting execution of one are named separately** (`yoyodyne-ifd.345`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **Human-facing Slack messages speak in titles and plain sentences, not ids** (`yoyodyne-ifd.68.15`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, and every distinction survives a terminal that cannot render emphasis.
- **The sink politely refuses unknown senders, once per thread** (`yoyodyne-ifd.68.21`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Inbound acknowledgments speak plainly, with ids nowhere a human reads** (`yoyodyne-ifd.68.26`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A thread settles its own pausing directive without naming an id** (`yoyodyne-ifd.68.27`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **README split tranche 4: citation sweep, link-map polish, and the CI walkthrough gate** (`yoyodyne-ifd.121.6`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Conversation and exchange records pin the account alias and configuration revision** (`yoyodyne-ifd.208`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Capability-based authorization: existing checks route through the registry, behavior-identical** (`yoyodyne-ifd.209.12`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Separation policy: authorship independence as explicit policy code** (`yoyodyne-ifd.209.13`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **The delivery definition's closing summaries say what closure now is** (`yoyodyne-ifd.209.25`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **Post-209.23 wording sweep: 'closed' where it now means only landed** (`yoyodyne-ifd.209.29`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **Every run-status renderer uses the what-happened-what-remains vocabulary** (`yoyodyne-ifd.226`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A run gets a per-run scratch directory, named in the contract, not an advisory naming convention** (`yoyodyne-ifd.247`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Release notes link to a landing target no restructure silently breaks** (`yoyodyne-ifd.315`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **The goal-structure checks move to the surface that can afford to be right** (`yoyodyne-ifd.326`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The goal-grading coverage 326 rests on is pinned** (`yoyodyne-ifd.328`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **The per-agent side-threads knob: configuration selects queue or side-threads, never authority** (`yoyodyne-ifd.330.3`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Convert the coordination slice to the workflow runtime at the management-requests conversion** (`yoyodyne-ifd.209.14`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.

## Bug fixes

- **The cap override the refusal text promises actually works, proven end to end** (`yoyodyne-ifd.231`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The watch session retries transient tracker reads and exits only on persistent failure** (`yoyodyne-ifd.232`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A blocked status nothing maintains cannot hide ready work from the scheduler** (`yoyodyne-ifd.277`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A confirmed note write is durable, or the confirmation is false** (`yoyodyne-ifd.336`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **The status probe observes leases without acquiring them** (`yoyodyne-ifd.200`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **The invariant loader skips the directory README, as everything else already documents** (`yoyodyne-ifd.201`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Codex's read-only claim matches what its sandbox prevents, or the descriptor stops claiming it** (`yoyodyne-ifd.202`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Stopping yoyo-status -L kills its background pipeline instead of orphaning it** (`yoyodyne-ifd.218`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **The harness sets GOCACHE for the runs it makes** (`yoyodyne-ifd.219`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Every Resolved() call site means has-a-disposition, proven by audit** (`yoyodyne-ifd.225`)
  Serves: Make user directives durable, discoverable, and enforceable regardless of which agent received them.
- **A late-reconciled stoppage still records why it stopped** (`yoyodyne-ifd.227`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A blocked run cannot read as succeeded: the outcome checks the blocker first** (`yoyodyne-ifd.233`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **A refused run-state save fails loudly, never leaving a stale record behind** (`yoyodyne-ifd.234`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **Review evidence includes the branch's committed work, not only uncommitted changes** (`yoyodyne-ifd.236`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **Land the unprompted-notices branch with the README count corrected** (`yoyodyne-ifd.249`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A turn that provably asked nothing gives its delivery attempt back** (`yoyodyne-ifd.251`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Escalation record writes survive a cancelled context** (`yoyodyne-ifd.253`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An epic with open execution children is not itself pulled as developer work** (`yoyodyne-ifd.256`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A settled escalation records its delivery even when the context dies** (`yoyodyne-ifd.257`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Confirm the parent-child edge direction against real tracker output** (`yoyodyne-ifd.259`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A single oversized output line is truncated, not fatal** (`yoyodyne-ifd.266`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The 121 double-run's actual mechanism is found before the epic guard is trusted** (`yoyodyne-ifd.273`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A failure report never claims preservation that did not happen** (`yoyodyne-ifd.275`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Work is not admitted twice from one source, and a satisfied item is caught before dispatch** (`yoyodyne-ifd.281`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Only one watch session runs per product: the watch takes a lease** (`yoyodyne-ifd.292`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Environment-independent refusals are reported before provider availability** (`yoyodyne-ifd.293`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Stall detection runs without Slack** (`yoyodyne-ifd.295`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A run's recorded reply accounts for the work, or the run does not end there** (`yoyodyne-ifd.307`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A timed-out tracker write says what landed: no more reported-failed-but-durable** (`yoyodyne-ifd.327`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A half-registered worktree cannot fail another run's worktree listing** (`yoyodyne-ifd.332`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Runs the harness launches cannot start a Git prune that eats a worktree registration** (`yoyodyne-ifd.334`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **A dropped queued merge is a durable event, said at warning, and counted by the heartbeat** (`yoyodyne-ifd.68.14`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, and every distinction survives a terminal that cannot render emphasis.
- **The design document's yoyo status entry matches what shipped** (`yoyodyne-ifd.162`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **A returned review round is credited only by the process that charged it** (`yoyodyne-ifd.196`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An over-length selection reason is truncated or refused loudly, never dropped silently** (`yoyodyne-ifd.197`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Concurrent runs cannot be served by the same pooled account: the rotation holds a lease** (`yoyodyne-ifd.198`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Revert the prompt-prefix reordering: ifd.84's null result, acted on** (`yoyodyne-ifd.204`)
  Serves: The operator can see what the harness spends on their behalf: provider-reported cost, per work item, per run, and in total.
- **A trial that could not start is never recorded as an unobserved run** (`yoyodyne-ifd.209.28`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **The terms check matches variants, and the governed scan re-runs variant-tolerant** (`yoyodyne-ifd.220`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
- **The refreshed export cannot become a run's own change: the skip-worktree guard is enforced, not assumed** (`yoyodyne-ifd.229`)
  Serves: Keep roles, policies, and provider selection configurable without making safety invariants optional.
- **Checkpoint timestamps are fixed-width, so the size-estimate test stops flaking** (`yoyodyne-ifd.235`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **The minute-zero probe does not race worktree materialization or a shared build cache** (`yoyodyne-ifd.238`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **The doctor's slack-secrets probe reads the variables, not just the file** (`yoyodyne-ifd.243`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **An oversized file in a change is shown as omitted, never silently absent from review** (`yoyodyne-ifd.245`)
  Serves: Every change is reviewed against the intent it claims to serve, by a role that did not write it, before it is integrated.
- **A hold or pause that starts nothing returns the re-run claim it consumed** (`yoyodyne-ifd.248`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Characterize the ConversationStore Hold/Release race flake** (`yoyodyne-ifd.254`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **An oversized work item degrades loudly in the bundle instead of becoming unrunnable** (`yoyodyne-ifd.263`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The tracker export cannot be smuggled into a run's committed change** (`yoyodyne-ifd.274`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The attribution audit sees closed work** (`yoyodyne-ifd.276`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The smoke check proves a real bd export carries a just-claimed item** (`yoyodyne-ifd.278`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **Secrets probes require the variables, not the file** (`yoyodyne-ifd.287`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The README tells the truth about how work gets pulled** (`yoyodyne-ifd.289`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **Configuration fails closed on an unrecognized role** (`yoyodyne-ifd.291`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The by-operator claim is enforced or the docs stop making it** (`yoyodyne-ifd.316`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **A test pins that the goals directory is refused to developer runs** (`yoyodyne-ifd.318`)
  Serves: Maintain a traceable chain from the product brief through goals, designs, work, code changes, and verification.
- **The CI adoption job pins its tracker version** (`yoyodyne-ifd.322`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **The adoption walkthrough refuses a scratch root inside a git repository** (`yoyodyne-ifd.323`)
  Serves: A newcomer can go from the documented install to a working first run on their own repository using the readme alone.
- **yoyo verbs work from inside a managed worktree** (`yoyodyne-ifd.335`)
  Serves: Run development nearly autonomously. The human's routine interface is the product manager: they state intent, approve the brief and goals, and answer questions the product manager escalates. Directing the architect, development manager, developer, or reviewer individually is available for inspection, recovery, and override, but is not part of the normal loop.
- **The first poll's improvement notices are batched, not one direct message each** (`yoyodyne-ifd.68.28`)
  Serves: The harness's surfaces read clearly: boundaries between topics and speakers are visible, important findings stand out, every distinction survives a terminal that cannot render emphasis, and user-facing language chooses the ordinary, literal word over metaphor, coinage, or term of art.
